[project]
name = "nidsdepoule-server"
version = "0.1.0"
description = "NidsDePoule pothole detection server"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "smoke: smoke tests against a live server (set SMOKE_TEST_URL)",
]
